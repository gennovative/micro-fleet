{
  "name": "@micro-fleet/common",
  "version": "1.2.2",
  "description": "Contains stuff used by other packages and micro services.",
  "scripts": {
    "build": "npm run compile && npm run test",
    "clean": "rimraf dist coverage .nyc_output",
    "dedup": "rimraf node_modules/**/node_modules/**/typings",
    "compile": "npm run clean && npm run tslint && tsc --pretty",
    "test": "nyc --reporter=json --reporter=html --include dist/app/**/*.js mocha --recursive dist/test",
    "tslint": "tslint -c tslint.json 'src/**/*.ts'",
    "watch": "npm run clean && npm run tslint && tsc --watch --pretty"
  },
  "dependencies": {
    "automapper-ts": "~1.8",
    "joi": "~13.0",
    "bluebird": "~3.5",
    "bluebird-global": "~1.0",
    "inversify": "~4.11",
    "lodash": "~4.17",
    "reflect-metadata": "~0.1"
  },
  "devDependencies": {
    "@types/bluebird-global": "~3.5",
    "@types/flake-idgen": "~0.1",
    "@types/joi": "~13.0",
    "@types/lodash": "~4.14"
 },
  "publishConfig": {
    "access": "public"
  },
  "nyc": {
    "extension": [
      ".ts",
      ".tsx"
    ],
    "exclude": [
      "**/*.d.ts"
    ],
    "reporter": [
      "html"
    ],
    "all": true,
    "sourceMap": true,
    "instrument": true
  },
  "author": {
    "name": "gennovative",
    "url": "https://github.com/gennovative"
  },
  "engines": {
    "node": "^8",
    "npm": "^5"
  },
  "license": "MIT",
  "private": false,
  "main": "./dist/app/index.js",
  "types": "./typings/app.d.ts"
}
